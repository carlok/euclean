# Does the control theory show concept survival?

Generated by `pipeline.ensemble.controlverdict`. The rule was fixed in code
before either grid ran.

**Verdict: SHOWS SURVIVAL**

## What is being compared

Availability: the largest fraction of grid members in whose candidate pool
any one canonical concept appears. Raw survival is not comparable across
theories, because a fixed top-k over a larger pool penalizes a richer
theory before any content is considered.

| source | control | incumbent | disjoint and higher |
|---|---|---|---|
| premise-conjunction | 0.867–0.911 (2 seeds) | 0.378–0.422 (3 seeds) | yes |
| quantified | 0.511–0.533 (2 seeds) | 0.356–0.356 (3 seeds) | yes |

## Conditions

| condition | passed | detail |
|---|---|---|
| some source is higher with disjoint ranges | yes | sources clearing the bar: ['premise-conjunction', 'quantified'] |
| control's search was not truncated, or cleared the bar anyway | yes | truncated but cleared the bar, which truncation biases against |
| both theories measured at the same budget | yes | budgets match |
| noise floor from at least 2 base seeds, both theories | yes | control 2, incumbent 3 |
| control grid is not mostly vacuous | yes | vacuous fraction 0.00, limit 0.33 |

## What this does not decide

Whether the scoring criteria pick better than chance once a concept is
available. On the incumbent that margin is indistinguishable from noise, so
it is reported but kept out of the rule.

